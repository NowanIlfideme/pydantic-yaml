# This is an example Conda environment, for local development
name: pyd-yaml
channels:
  - conda-forge
dependencies:
  - python=3.10 # docs need 3.10
  - pip
  - pip:
      - -e ".[dev,docs]"
      - pydantic>=2.0.0
