"""Reader API.

The intent is to allow reading a YAML file (or stream) from one or more sources,
with the aviailability of multiple options (such as YAML 1.1 or 1.2 compatibility)
and even integrating other libraries (pyyaml, ruamel.yaml, omegaconf, etc.)
"""
